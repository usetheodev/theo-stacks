version: 1
project: "{{project-name}}"

commands:
  dev: "npm run dev"
  build: "npm run build"
  test: "npm test"
  lint: "npm run lint"
  format: "npm run format"
  security: "npm audit"

apps:
  web:
    path: .
    framework: nextjs
    type: frontend
    render: server
