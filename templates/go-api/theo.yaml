version: 1
project: "{{project-name}}"

commands:
  dev: "go run ."
  build: "go build -o bin/server ."
  test: "go test ./..."
  lint: "go vet ./..."
  format: "gofmt -w ."
  security: "govulncheck ./..."

apps:
  api:
    path: .
    framework: custom
    type: server
    port: 8080
