version: 1
project: "node-express"

commands:
  dev: "npm run dev"
  build: "npm run build"
  test: "npm test"
  lint: "npm run lint"
  format: "npm run format"
  security: "npm audit"

apps:
  api:
    path: .
    framework: express
    type: server
    port: 3000
